public class TwoRoot 
{
    public static void main(String[] args) 
    {
		double twoRoot = Math.sqrt(2.0);
		System.out.println("The square root of 2 is " + twoRoot);
		
		// If we want to check the accuracy of the result (it's not 2 exactly)
		// System.out.println("That root squared is " + (twoRoot * twoRoot));
    }
}
